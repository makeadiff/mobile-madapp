'use strict';

/**
 * @ngdoc function
 * @name mobileApp.controller:FaqCtrl
 * @description
 * # FAQCtrl
 * Controller of the mobileApp
 */
angular.module('mobileApp')
  .controller('FaqCtrl', ['$scope', '$location', '$http', function ($scope, $location, $http) {
    var FaqCtrl = this;

  }]);
